function quickSort(arr){

}


quickSort([5,1,2,10])


/*
need a wall, pivot, current
recusion:
+) if current < pivot, swap current with wall
+) increase wall;

out of the loop, swap pivot and wall
loop from start -> wall -1
loop from wall -> end

return arr


*/
